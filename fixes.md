# Pathway v0.1.0 notes

## AI

- Auto generation should pop up a modal that shows AI generated tasks and user should be able to select the ones wanted

## Visuals

- Lightness bar should be able to go SLIGHTLY darker
- In daily generation selected tasks SHOULD NOT cross themselves out visually
- REMOVE SCROLL BAR

## Settings

- Add daily reset time parameter (defauly 4 am) in settings
- Order of pages in nav bar should be configurable (except for done always last), done should be removable actually

## QOL

- when pressing enter and creating new task, the new tasks parameters should go to 1 1 1 not copy the one above
- In the daily task generation pages sometimes when tasks have the same name they all get ticked at once if chosen

## Trackers page

- new tracker page. quite simple trackers should just have a completion number, a current number and an importance metric in the tracker page each tracker should have a prgress bar, % and fraction, and a - and + button. The entire tracker page should be operated by a single markdown, accessible from the main tracker page.
